import { PassMatchDirective } from './pass-match.directive';

describe('PassMatchDirective', () => {
  it('should create an instance', () => {
    const directive = new PassMatchDirective();
    expect(directive).toBeTruthy();
  });
});
